class C {
  <T> T m(T o) { return null; }
  <S> S m() { return null; }
  Integer foo() { 
      Boolean b = m(null);
      return m(null); 
  }
  Integer bar() { 
      Boolean b = m();
      return m(); 
  }
}
