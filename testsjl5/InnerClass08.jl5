class TreeMap<K> { }
class NavSubmap<K> {
    class SubmapKi { 
        SubmapKi(TreeMap<K> o) {
        }
        void bar(TreeMap<K> o) {
        }
    }
}
class AscSubmap<K> extends NavSubmap<K> {
    public void foo(SubmapKi c, TreeMap<K> a) {
        // both of these calls appear 
        // to fail for the same reason.
        SubmapKi b = new SubmapKi(a);
        c.bar(a);
    }
}
